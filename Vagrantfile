# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  config.vm.box = "lantrix/amazonlinux2"
  config.vm.box_version = "2018.11.26"
  config.vm.define :otgstack_dev
  config.vm.provider :virtualbox
end